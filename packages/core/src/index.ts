/**
 * @md/core — inline-WYSIWYG markdown editing for CodeMirror 6.
 *
 * Invariant I1: the EditorState doc holds the raw markdown, byte for byte.
 * Rendering happens only through decorations layered over that text.
 */
export const version = '0.1.0'
